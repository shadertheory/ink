support whole closure, and per capture copy/clone. in addition make copy clone a fundamental part of the language like Rust but they should be keywords that resolve to the trait. make them inference only.

do option 1 await returns result<T, error>

1. default capture mode should work like rust
2. yes exactly but ref should be for dereferencing an expression like rust and & should be for matching to or specifying a reference (&[]T instead of ref []T. ref []T would be used in match case or if let like rust)

